order: 1
id: outlook-recipients-and-attendees-get-from-message-read
name: Get from (Message Read)
description: Gets who the message is from in Read mode.
host: OUTLOOK
api_set:
    Mailbox: '1.1'
script:
    content: |-
        document.getElementById("get-from").addEventListener("click", getFrom);

        function getFrom() {
          const msgFrom = Office.context.mailbox.item.from;
          console.log("Message received from: " + msgFrom.displayName + " (" + msgFrom.emailAddress + ")");
        }
    language: typescript
template:
    content: |-
        <section>
            <p>This sample shows how to get who an email is <b>from</b>. In a delegate access scenario, the
                <b>from</b> property represents the delegator. <b>Tip</b>: Use <b>sender</b> to get the delegate.</p>
            <p><b>Required mode</b>: Message Read</p>
        </section>
        <section>
            <h3>Try it out</h3>
            <button id="get-from" >Who is this from?</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #0078d4;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #106ebe;
        }

        button:active {
            background-color: #005a9e;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts