order: 2
id: excel-custom-xml-parts-test-xml-for-unique-namespace
name: Unique namespaces in custom XML
description: Tests to see if there is only one XML part for a specified namespace.
author: OfficeDev
host: EXCEL
api_set:
    ExcelApi: '1.5'
script:
    content: |-
        document.getElementById("create-custom-xml-part").addEventListener("click", () => tryCatch(createCustomXmlPart));
        document.getElementById("test-for-unique-namespace").addEventListener("click", () => tryCatch(testForUniqueNamespace));
        document.getElementById("delete-all-custom-xml-parts").addEventListener("click", () => tryCatch(deleteAllCustomXmlParts));

        async function createCustomXmlPart() {
            await Excel.run(async (context) => {
                document.getElementById("display-xml").textContent = "";

                // You must have the xmlns attribute to populate the 
                // CustomXml.namespaceUri property.
                const originalXml = "<Reviewers xmlns='http://schemas.contoso.com/review/1.0'><Reviewer>Juan</Reviewer><Reviewer>Hong</Reviewer><Reviewer>Sally</Reviewer></Reviewers>";
                const customXmlPart = context.workbook.customXmlParts.add(originalXml);
                const xmlBlob = customXmlPart.getXml();

                await context.sync();

                // Make it a bit more readable.
                const readableXml = xmlBlob.value.replace(/></g, ">\n<");
                document.getElementById("display-xml").textContent = readableXml;

                await context.sync();
            });
        }

        async function testForUniqueNamespace() {
            await Excel.run(async (context) => {
                document.getElementById("display-xml").textContent = "";
                const contosoNamespace = "http://schemas.contoso.com/review/1.0";
                const customXmlParts = context.workbook.customXmlParts;
                const filteredXmlParts = customXmlParts.getByNamespace(contosoNamespace);
                const numberOfPartsInNamespace = filteredXmlParts.getCount();

                await context.sync();

                if (numberOfPartsInNamespace.value == 1) {
                    const onlyXmlPartInNamespace = filteredXmlParts.getOnlyItem();
                    const xmlBlob = onlyXmlPartInNamespace.getXml();

                    await context.sync();

                    // Make it a bit more readable.
                    const readableXml = xmlBlob.value.replace(/></g, ">\n<");

                    document.getElementById("display-xml").textContent = `The only XML part in the namespace ${contosoNamespace} is:
                        ${readableXml}`;

                } else {
                    console.log(`There are ${numberOfPartsInNamespace.value} XML parts with namespace ${contosoNamespace}. There should be exactly 1.`);
                }        

                await context.sync();
            });
        }

        async function deleteAllCustomXmlParts() {
            await Excel.run(async (context) => {
                document.getElementById("display-xml").textContent = "";
                const customXmlParts = context.workbook.customXmlParts;
                customXmlParts.load("items");

                await context.sync();

                for (let i = 0; i < customXmlParts.items.length; i++) {
                    customXmlParts.items[i].delete();
                }

                await context.sync();
            });
        }

        /** Default helper for invoking an action and handling errors. */
        async function tryCatch(callback) {
            try {
                await callback();
            }
            catch (error) {
                // Note: In a production add-in, you'd want to notify the user through your add-in's UI.
                console.error(error);
            }
        }
    language: typescript
template:
    content: |-
        <section>
            <p>This sample shows how to determine if there is just one XML part for a specified namespace.</p>
        </section>
        <section>
            <h3>Try it out</h3>
            <p>Press the"Create XML part" button to create and display Contoso's Reviewer metadata. <span >Press it more than once if you want to set up an error situation.</span></p>
            <button id="create-custom-xml-part">Create XML part</button>
            <p>Press the"Test for unique namespace" button to see if there is more than one XML part with the Contoso namespace. <span >If there is more than one, an error is thrown.</span></p>
            <button id="test-for-unique-namespace">Test for unique namespace</button>
                <p>To start over, press"Delete all XML parts".</p>
            <button id="delete-all-custom-xml-parts">Delete all XML parts</button>
        </section>
        <section>
            <h3>XML part display</h3>
            <div id="display-xml" style="max-width:320px;">
            </div>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #0078d4;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #106ebe;
        }

        button:active {
            background-color: #005a9e;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts