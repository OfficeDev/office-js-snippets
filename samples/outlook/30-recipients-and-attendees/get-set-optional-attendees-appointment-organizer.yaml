order: 12
id: outlook-recipients-and-attendees-get-set-optional-attendees-appointment-organizer
name: Get and set optional attendees (Appointment Organizer)
description: Gets and sets the optional attendees in Appointment Organizer mode.
host: OUTLOOK
api_set:
    Mailbox: '1.1'
script:
    content: |-
        document.getElementById("get-optional-attendees").addEventListener("click", getOptionalAttendees);
        document.getElementById("set-optional-attendees").addEventListener("click", setOptionalAttendees);

        function getOptionalAttendees() {
          Office.context.mailbox.item.optionalAttendees.getAsync(function(asyncResult) {
            if (asyncResult.status === Office.AsyncResultStatus.Succeeded) {
              const apptOptionalAttendees = asyncResult.value;
              for (let i = 0; i < apptOptionalAttendees.length; i++) {
                console.log(
                  "Optional attendees: " +
                    apptOptionalAttendees[i].displayName +
                    " (" +
                    apptOptionalAttendees[i].emailAddress +
                    ") - response: " +
                    apptOptionalAttendees[i].appointmentResponse
                );
              }
            } else {
              console.error(asyncResult.error);
            }
          });
        }

        function setOptionalAttendees() {
          const email = (document.getElementById("emailOptional") as HTMLInputElement).value;
          const emailArray = [email];
          Office.context.mailbox.item.optionalAttendees.setAsync(emailArray, function(asyncResult) {
            if (asyncResult.status === Office.AsyncResultStatus.Succeeded) {
              console.log("Succeeded in setting optional attendees field.");
            } else {
              console.error(asyncResult.error);
            }
          });
        }
    language: typescript
template:
    content: |-
        <section>
            <p>This sample shows how to get and set the <b>optional attendees</b>.</p>
            <p><b>Required mode</b>: Appointment Organizer</p>
        </section>
        <section>
            <h3>Try it out</h3>
            <label>Email address</label>
            <input id="emailOptional" type="text" value="" placeholder="Who do you want to invite as an optional attendee?">
                <button id="set-optional-attendees">Set optional attendees</button>
                <button id="get-optional-attendees">Get optional attendees</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #f0f0f0;
            color: #333333;
            border: 1px solid #8a8a8a;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #e0e0e0;
        }

        button:active {
            background-color: #d0d0d0;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts