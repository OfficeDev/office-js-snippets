order: 11
id: outlook-recipients-and-attendees-get-optional-attendees-appointment-attendee
name: Get optional attendees (Appointment Attendee)
description: Gets the optional attendees in Appointment Attendee mode.
host: OUTLOOK
api_set:
    Mailbox: '1.1'
script:
    content: |-
        document.getElementById("get-optional-attendees").addEventListener("click", getOptionalAttendees);

        function getOptionalAttendees() {
          const apptOptionalAttendees = Office.context.mailbox.item.optionalAttendees;
          console.log("Optional attendees:");
          for (let i = 0; i < apptOptionalAttendees.length; i++) {
            console.log(
              apptOptionalAttendees[i].displayName +
                " (" +
                apptOptionalAttendees[i].emailAddress +
                ") - response: " +
                apptOptionalAttendees[i].appointmentResponse
            );
          }
        }
    language: typescript
template:
    content: |-
        <section class="description">
            <p>This sample shows how to get the <b>optional attendees</b>.</p>
            <p><b>Required mode</b>: Appointment Attendee</p>
        </section>
        <section class="samples">
            <h3>Try it out</h3>
            <button id="get-optional-attendees" >Who are the optional attendees?</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #0078d4;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #106ebe;
        }

        button:active {
            background-color: #005a9e;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts