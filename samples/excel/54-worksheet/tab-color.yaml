order: 12
id: excel-worksheet-tab-color
name: Tab color
description: Gets and sets the tab color of a worksheet.
author: OfficeDev
host: EXCEL
api_set:
    ExcelApi: '1.7'
script:
    content: |-
        document.getElementById("set-tab-color-to-hex-color").addEventListener("click", () => tryCatch(setTabColorToHexColor));
        document.getElementById("set-tab-color-to-named-color").addEventListener("click", () => tryCatch(setTabColorToNamedColor));
        document.getElementById("set-tab-color-to-default-color").addEventListener("click", () => tryCatch(setTabColorToDefaultColor));
        document.getElementById("get-tab-color").addEventListener("click", () => tryCatch(getTabColor));

        async function setTabColorToHexColor() {
            await Excel.run(async (context) => {
                const activeSheet = context.workbook.worksheets.getActiveWorksheet();
                activeSheet.tabColor = "#FF0000";

                await context.sync();
            });
        }

        async function setTabColorToNamedColor() {
            await Excel.run(async (context) => {
                const activeSheet = context.workbook.worksheets.getActiveWorksheet();
                activeSheet.tabColor = "yellow";

                await context.sync();
            });
        }

        async function setTabColorToDefaultColor() {
            await Excel.run(async (context) => {
                const activeSheet = context.workbook.worksheets.getActiveWorksheet();
                activeSheet.tabColor = "";

                await context.sync();
            });
        }

        async function getTabColor() {
            await Excel.run(async (context) => {
                const activeSheet = context.workbook.worksheets.getActiveWorksheet();
                activeSheet.load("name, tabColor");

                await context.sync();

                let tabColor = activeSheet.tabColor === "" ? "the default color" : activeSheet.tabColor;
                console.log(`Tab color for the "${activeSheet.name}" worksheet is ${tabColor}.`);
            });
        }

        /** Default helper for invoking an action and handling errors. */
        async function tryCatch(callback) {
            try {
                await callback();
            }
            catch (error) {
                // Note: In a production add-in, you'd want to notify the user through your add-in's UI.
                console.error(error);
            }
        }
    language: typescript
template:
    content: |-
        <section>
            <p>This sample shows how to set and get the tab color of a worksheet.</p>
        </section>
        <section>
            <h3>Try it out</h3>
            <label>Set tab color for the active worksheet.</label>
            <button id="set-tab-color-to-hex-color" >Set tab color to hex color</button>
            <button id="set-tab-color-to-named-color" >Set tab color to named color</button>
            <button id="set-tab-color-to-default-color" >Set tab color to default color</button>
            <br/><br/>
            <label>Get tab color of the active worksheet.</label>
            <button id="get-tab-color" >Get tab color</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #f0f0f0;
            color: #333333;
            border: 1px solid #8a8a8a;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #e0e0e0;
        }

        button:active {
            background-color: #d0d0d0;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts