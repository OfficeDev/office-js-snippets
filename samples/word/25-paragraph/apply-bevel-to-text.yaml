order: 14
id: word-paragraph-apply-bevel-to-text
name: Apply a bevel to text
description: Applies a bevel effect to text in the document.
author: OfficeDev
host: WORD
api_set:
    WordApiDesktop: '1.3'
script:
    content: |
        document.getElementById("run").addEventListener("click", () => tryCatch(run));

        async function run() {
          if (!Office.context.requirements.isSetSupported("WordApiDesktop", "1.3")) {
            console.error("This sample requires WordApiDesktop 1.3 or later.");
            return;
          }

          await Word.run(async (context) => {
            const paragraph: Word.Paragraph = context.document.body.insertParagraph(
              "Beveled text",
              Word.InsertLocation.start,
            );
            paragraph.font.size = 48;
            paragraph.font.bold = true;

            const threeDimensionalFormat: Word.ThreeDimensionalFormat = paragraph.font.threeDimensionalFormat;
            threeDimensionalFormat.isVisible = true;
            threeDimensionalFormat.bevelTopType = Word.BevelType.circle;
            threeDimensionalFormat.bevelTopDepth = 6;
            threeDimensionalFormat.bevelTopInset = 6;
            threeDimensionalFormat.load("bevelTopType,bevelTopDepth,bevelTopInset");

            await context.sync();

            console.log("Applied bevel formatting:", {
              type: threeDimensionalFormat.bevelTopType,
              depth: threeDimensionalFormat.bevelTopDepth,
              inset: threeDimensionalFormat.bevelTopInset,
            });
          });
        }

        // Default helper for invoking an action and handling errors.
        async function tryCatch(callback) {
          try {
            await callback();
          } catch (error) {
            // Note: In a production add-in, you'd want to notify the user through your add-in's UI.
            console.error(error);
          }
        }
    language: typescript
template:
    content: |-
        <section>
            <p>Insert text and apply a circular bevel effect.</p>
            <button id="run">Apply bevel</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #f0f0f0;
            color: #333333;
            border: 1px solid #8a8a8a;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #e0e0e0;
        }

        button:active {
            background-color: #d0d0d0;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts