order: 10
id: excel-basics-api-call-with-notification-toast
name: API call with Fabric notification toast
description: Highlights the currently-selected range and reports its address via Fabric Message Banner
host: EXCEL
api_set: {}
script:
    content: |-
        $("#run").click(run);

        /**
         * This sample will color the selected range in yellow,
         * and also display the selection address.
         */
        async function run() {
            try {
                await Excel.run(async (context) => {
                    const range = context.workbook.getSelectedRange();
                    range.format.fill.color = "yellow";
                    range.load("address");

                    await context.sync();

                    showNotification(`The selection address is "${range.address}".`);
                });
            }
            catch (error) {
                showNotification(error.message || error.toString());
                OfficeHelpers.Utilities.log(error);
            }
        }

        /**
         * Display the notification having synced the changes.
         */
        function showNotification(message: string) {
            const messageBanner = $('.ms-MessageBanner');
            $('.ms-MessageBanner-clipper').text(message);
            $('.ms-MessageBanner-close').click(() => {
                messageBanner.hide();
                messageBanner.off('click');
            });
            messageBanner.show();
        }
    language: typescript
template:
    content: "<!-- Fabric Message Banner template -->\n<!-- https://dev.office.com/fabric-js/Components/MessageBanner/MessageBanner.html -->\n<div class=\"ms-MessageBanner\">\n\t<div class=\"ms-MessageBanner-content\">\n\t\t<div class=\"ms-MessageBanner-text\">\n\t\t\t<div class=\"ms-MessageBanner-clipper\"></div>\n\t\t</div>\n\t</div>\n\t<button class=\"ms-MessageBanner-close\">\n    <i class=\"ms-Icon ms-Icon--Clear\"></i>\n  </button>\n</div>\n\n<section id=\"main\" class=\"ms-font-m\">\n\t<p>Color the selected range in yellow, and display the selection address in a notification toast.</p>\n\t<button id=\"run\" class=\"ms-Button\">\n        <span class=\"ms-Button-label\">Run code</span>\n    </button>\n</section>"
    language: html
style:
    content: |
        /** Remove margin and padding from body, to line up the notification nicely. Instead, use a section for the main content, and set the margin there. */

        body {
            margin: 0;
            padding: 0;
        }

        #main {
            margin: 10px;
        }

        .ms-MessageBanner {
            display: none;
        }
    language: css
libraries: |
    # Office.js
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js

    # CSS Libraries
    office-ui-fabric-js@1.4.0/dist/css/fabric.min.css
    office-ui-fabric-js@1.4.0/dist/css/fabric.components.min.css

    # NPM libraries
    core-js@2.4.1/client/core.min.js
    @microsoft/office-js-helpers@0.6.0/dist/office.helpers.min.js
    jquery@3.1.1

    # IntelliSense: @types/library or node_modules paths or URL to d.ts files
    @types/office-js
    @types/core-js
    @microsoft/office-js-helpers/dist/office.helpers.d.ts
    @types/jquery
