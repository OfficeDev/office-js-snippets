order: 26
id: outlook-get-in-reply-to
name: Get the ID of the message being replied to (Message Compose)
description: Retrieves the ID of the message being replied to by the current message.
host: OUTLOOK
api_set:
    Mailbox: '1.14'
script:
    content: |-
        document.getElementById("get-in-reply-to").addEventListener("click", getInReplyTo);

        function getInReplyTo() {
          // This snippet gets the ID of the message being replied to by the current message (PR_IN_REPLY_TO_ID).
          // The API call is supported on messages being composed and isn't supported on read items.
          const inReplyTo = Office.context.mailbox.item.inReplyTo;
          if (inReplyTo) {
            console.log("ID of the message being replied to: " + inReplyTo);
          } else {
            console.log("No InReplyTo property available for this message");
          }
        }
    language: typescript
template:
    content: |-
        <section class = "description">
            <p>This sample shows how to get the ID of the message being replied to by the current message.</p>
            <p><b>Required mode</b>: Message Compose</p>
        </section>
        <section class = "samples">
            <h3>Try it out</h3>
            <button id = "get-in-reply-to" >Get the ID</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #0078d4;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #106ebe;
        }

        button:active {
            background-color: #005a9e;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts