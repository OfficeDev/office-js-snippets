order: 2
id: outlook-categories-work-with-master-categories
name: Work with the categories master list
description: Gets, adds, and removes categories in the master list for the mailbox.
host: OUTLOOK
api_set:
    Mailbox: '1.8'
script:
    content: |-
        document.getElementById("get-master-categories").addEventListener("click", getMasterCategories);
        document.getElementById("add-master-categories").addEventListener("click", addMasterCategories);
        document.getElementById("remove-master-categories").addEventListener("click", removeMasterCategories);

        function getMasterCategories() {
          Office.context.mailbox.masterCategories.getAsync(function(asyncResult) {
            if (asyncResult.status === Office.AsyncResultStatus.Succeeded) {
              const categories = asyncResult.value;
              if (categories && categories.length > 0) {
                console.log("Master categories:");
                console.log(JSON.stringify(categories));
              } else {
                console.log("There are no categories in the master list.");
              }
            } else {
              console.error(asyncResult.error);
            }
          });
        }

        function addMasterCategories() {
          const masterCategoriesToAdd = [
            {
              displayName: "TestCategory",
              color: Office.MailboxEnums.CategoryColor.Preset0
            }
          ];

          Office.context.mailbox.masterCategories.addAsync(masterCategoriesToAdd, function(asyncResult) {
            if (asyncResult.status === Office.AsyncResultStatus.Succeeded) {
              console.log("Successfully added categories to master list");
            } else {
              console.log("masterCategories.addAsync call failed with error: " + asyncResult.error.message);
            }
          });
        }

        function removeMasterCategories() {
          const masterCategoriesToRemove = ["TestCategory"];

          Office.context.mailbox.masterCategories.removeAsync(masterCategoriesToRemove, function(asyncResult) {
            if (asyncResult.status === Office.AsyncResultStatus.Succeeded) {
              console.log("Successfully removed categories from master list");
            } else {
              console.log("masterCategories.removeAsync call failed with error: " + asyncResult.error.message);
            }
          });
        }
    language: typescript
template:
    content: |-
        <section>
            <p>This sample shows how to get, add, and remove <b>master categories</b> on the mailbox.</p>
        </section>
        <section>
            <h3>Try it out</h3>
            <button id="add-master-categories">Add category to master list</button>
            <button id="get-master-categories">Get categories master list</button>
            <button id="remove-master-categories">Remove category from master list</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #f0f0f0;
            color: #333333;
            border: 1px solid #8a8a8a;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #e0e0e0;
        }

        button:active {
            background-color: #d0d0d0;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts