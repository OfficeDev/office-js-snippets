order: 8
id: excel-workbook-save-and-close
name: Save and close
description: Saves and closes a workbook.
host: EXCEL
api_set:
    ExcelAPI: '1.11'
script:
    content: |-
        document.getElementById("saveWithPrompt").addEventListener("click", () => tryCatch(saveWithPrompt));
        document.getElementById("saveWithoutPrompt").addEventListener("click", () => tryCatch(saveWithoutPrompt));
        document.getElementById("closeWithSave").addEventListener("click", () => tryCatch(closeWithSave));
        document.getElementById("closeWithoutSave").addEventListener("click", () => tryCatch(closeWithoutSave));

        async function saveWithPrompt() {
            await Excel.run(async (context) => {
                context.workbook.save(Excel.SaveBehavior.prompt);
            });
        }

        async function saveWithoutPrompt() {
            await Excel.run(async (context) => {
                context.workbook.save(Excel.SaveBehavior.save);
            });
        }

        async function closeWithSave() {
            await Excel.run(async (context) => {
                context.workbook.close(Excel.CloseBehavior.save);
            });
        }

        async function closeWithoutSave() {
            await Excel.run(async (context) => {
                context.workbook.close(Excel.CloseBehavior.skipSave);
            });
        }

        /** Default helper for invoking an action and handling errors. */
        async function tryCatch(callback) {
            try {
                await callback();
            } catch (error) {
                // Note: In a production add-in, you'd want to notify the user through your add-in's UI.
                console.error(error);
            }
        }
    language: typescript
template:
    content: |-
        <section class="description">
            <p>This sample shows how to save a workbook, both with and without a prompt to the user, and how to close the workbook.</p>
        </section>
        <section class="samples">
            <h3>Try it out</h3>
            <button id="saveWithPrompt" >Save (with a first-save prompt)</button><p/>
            <button id="saveWithoutPrompt" >Save (without any prompt)</button><p/>
            <button id="closeWithSave" >Close the workbook (and save)</button><p/>
                <button id="closeWithoutSave" >Close the workbook (without saving)</button>
        </section>
    language: html
style:
    content: |-
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 14px;
            line-height: 1.5;
            padding: 10px;
        }

        section {
            margin-bottom: 20px;
        }

        h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 16px;
        }

        p {
            margin: 0 0 10px 0;
        }

        button {
            background-color: #0078d4;
            color: white;
            border: none;
            padding: 8px 16px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 2px;
            margin-left: 20px;
            margin-bottom: 5px;
            min-width: 80px;
            display: block;
        }

        button:hover {
            background-color: #106ebe;
        }

        button:active {
            background-color: #005a9e;
        }

        input {
            padding: 8px;
            margin: 5px 0;
            border: 1px solid #ccc;
            border-radius: 2px;
            font-size: 14px;
        }

        .header {
            text-align: center;
            background-color: #f3f2f1;
            padding: 10px;
        }
    language: css
libraries: |-
    https://appsforoffice.microsoft.com/lib/1/hosted/office.js
    https://raw.githubusercontent.com/DefinitelyTyped/DefinitelyTyped/master/types/office-js/index.d.ts